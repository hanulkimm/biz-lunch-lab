# ─── Web framework ───
fastapi
uvicorn[standard]

# ─── Config / validation ───
pydantic
pydantic-settings
python-dotenv

# ─── Auth ───
python-jose[cryptography]
bcrypt

# ─── Database (Supabase / PostgreSQL) ───
supabase

# ─── AI / RAG ───
anthropic
openai
pinecone
langchain
langchain-anthropic
langchain-openai

# ─── HTTP (Kakao API) ───
httpx

# ─── 파일 업로드 (닮은꼴 주민 사진 분석) ───
python-multipart

# ─── 시간대 데이터 (낚시터 KST 판정 — Windows/slim 이미지엔 내장 tz DB 없음) ───
tzdata
